# 인스타그램 카드뉴스 제작 가이드 — 오늘의 유튜브 이슈영상 순위

사용자가 유튜브 이슈분석 엑셀 파일(예: `YouTube이슈분석_개인채널_YYYY-MM-DD.xlsx`)을 지정하면,
그 파일에서 영상을 선정해 인스타그램 카드뉴스를 자동 제작하는 가이드입니다.

- **제작 도구**: Canva MCP (`generate-design-structured` → `export-design`)
- **참고 디자인**: `카드뉴스_instagram 샘플/` 폴더의 tripgoing 카드뉴스 4장
  (전면 사진 배경 + 상/하단 어두운 그라데이션 + 굵은 흰색 한글 타이포)
- **가독성 조정**: 샘플 대비 **폰트 크기 약 20~30% 확대** (아래 타이포 스케일 참조)

---

## 1. 입력 데이터 추출 규칙

지정된 엑셀 파일의 구조 (시트 2개):

| 시트 | 내용 | 주요 컬럼 |
|---|---|---|
| `전체` | 국가×주제별 이슈영상 | 날짜, 국가, 주제, 순위, 제목, 채널, 게시일, 길이, 조회수, 좋아요, 댓글, 이슈점수, 상세요약, 도움내용, 영상링크(하이퍼링크), 분석방식 |
| `라이징스타` | 급상승 신흥 채널 영상 | 위와 동일 (이슈점수는 0일 수 있음) |

> ⚠️ 로컬 환경에서 openpyxl이 numpy 문제로 실패할 수 있음.
> 그 경우 xlsx를 zip으로 열어 `xl/sharedStrings.xml` + `xl/worksheets/sheetN.xml`을
> 직접 파싱하고, 영상 링크는 `xl/worksheets/_rels/sheetN.xml.rels`의 하이퍼링크에서 추출한다.

### 영상 선정 기준

1. **🇰🇷 한국 TOP 3**: `전체` 시트에서 `국가=한국` 인 행을 **이슈점수 내림차순** 정렬 후 상위 3개
   (주제 구분 없이 전체에서 선정)
2. **🇯🇵 일본 TOP 3**: 같은 방식으로 `국가=일본` 상위 3개
3. **⭐ 라이징스타 1개**: `라이징스타` 시트의 순위 1위 영상
   (사용자가 특정 국가를 원하면 해당 국가의 최상위 라이징 영상으로 교체)

### 카드 제작에 필요한 필드 (영상당)

- 제목 (길면 카드용으로 1줄 요약 축약본 별도 작성)
- 채널명 (일본 채널은 원문 표기 유지)
- 조회수 (만 단위 축약: `310.8만`)
- 이슈점수 (만 단위 축약)
- 상세요약 → 카드용 2행 설명문(38px 기준 약 40자 내외)으로 요약
- 영상링크 → VIDEO_ID 추출
- 배경 이미지: `https://i.ytimg.com/vi/<VIDEO_ID>/maxresdefault.jpg` (404 시 `hqdefault.jpg` 폴백)

---

## 2. 공통 디자인 시스템

| 항목 | 값 |
|---|---|
| 캔버스 | 1080 × 1350 px (인스타 4:5), 총 8장 캐러셀 |
| 배경 | 영상 썸네일 전면(full-bleed) + 상·하단 검정 그라데이션 오버레이 (위 0→40%, 아래 0→75% 불투명) |
| 헤드라인 폰트 | Pretendard Black 또는 Noto Sans KR Black, 흰색 #FFFFFF, 자간 -2% |
| 본문 폰트 | Noto Sans KR Medium, #F2F2F2 |
| 포인트 컬러 | 레드 #FF3B30 (순위 뱃지·라이징 강조), 옐로 #FFD60A (라이징 별) |
| 로고 뱃지 | 좌상단 `HOT YouTube` 필박스 (검정 배경 + 흰 텍스트, 샘플의 trip going 뱃지 위치와 동일) |
| 푸터 | `Korea ─────── ▶ ─────── Japan` 점선 구분 워드마크 (샘플 하단 스타일), 40px, 60% 불투명 |
| 텍스트 안전영역 | 좌우 72px, 상하 96px 마진 안쪽 |

### 타이포 스케일 (샘플 대비 확대)

| 요소 | 샘플 추정 | **적용 크기** |
|---|---|---|
| 표지 메인 타이틀 | ~72px | **96px / 행간 1.15** |
| 표지 순위 리스트 | — | **40px 제목 / 32px 채널명** |
| 본문 카드 영상 제목 | ~56px | **72px / 행간 1.2** |
| 본문 카드 채널·국기 라벨 | ~32px | **44px** |
| 본문 카드 설명 2줄 | ~28px | **38px / 행간 1.5** |
| 순위 뱃지 숫자 | — | **120px** |

---

## 3. 카드별 레이아웃 (총 8장)

### 카드 1 — 표지

- **배경**: 한국 1위 영상 썸네일 확대 + 전체 어두운 오버레이(55%)
- **상단**: 로고 뱃지 + 날짜 칩 `YYYY.MM.DD` (엑셀의 날짜 컬럼 사용)
- **메인 타이틀** (96px, 좌측 정렬, 샘플 표지처럼 상단 1/3 지점, 2행 배치):

  > **오늘의
  > 유튜브 이슈영상 순위**

- **하단 순위 리스트** (반투명 검정 패널 위, 각 제목은 1줄 축약 + 말줄임, 채널명 병기):

  ```
  🇰🇷 한국
  1️⃣ {한국1위 축약제목} · {채널명}
  2️⃣ {한국2위 축약제목} · {채널명}
  3️⃣ {한국3위 축약제목} · {채널명}

  🇯🇵 일본
  1️⃣ {일본1위 축약제목} · {채널명}
  2️⃣ {일본2위 축약제목} · {채널명}
  3️⃣ {일본3위 축약제목} · {채널명}

  ⭐ 라이징 | {라이징 축약제목} · {채널명}
  ```

- **푸터**: `Korea ─── ▶ ─── Japan` 워드마크 + `1/8` 페이지 표기

### 카드 2~4 — 🇰🇷 한국 1·2·3위

샘플 본문1·본문2와 동일 구조(하단 중앙 정렬 텍스트 블록):

- 배경: 해당 영상 썸네일 full-bleed
- 좌상단: 빨간 원형 **순위 뱃지** (120px 숫자 `1`/`2`/`3`)
- 하단 텍스트 블록 (아래에서 160px 위):
  - 라벨(44px): `🇰🇷 한국 N위 · {채널명}`
  - 제목(72px Bold, 최대 2행): 축약 제목
  - 설명(38px, 2행): `상세요약`을 요약한 문장
  - 메타(36px, 70% 불투명): `조회수 {N}만 · 이슈점수 {N}만`
- 페이지 번호 `N/8`

### 카드 5~7 — 🇯🇵 일본 1·2·3위

카드 2~4와 동일 레이아웃, 라벨만 `🇯🇵 일본 N위 · {채널명}` (채널명 원문 표기).

### 카드 8 — ⭐ 라이징스타

- 배경: 라이징 영상 썸네일 + 딥네이비 오버레이
- 상단 중앙: ⭐ + `오늘의 라이징 스타` (옐로 #FFD60A, 56px)
- 제목(72px): 축약 제목
- 라벨(44px): `{국기} {채널명} · 조회수 {N}만`
- 설명(38px): 상세요약 기반 + "급상승 중인 신흥 채널" 멘트
- 하단 CTA(40px): `매일 업데이트되는 이슈 순위 → 팔로우·저장 ✅`

---

## 4. 게시물 캡션 템플릿 (표지_설명글 샘플 스타일)

```
오늘({M/D})의 유튜브 이슈영상 순위를 소개합니다 📊
🇰🇷 한국은 {한국1위 채널}의 {한국1위 키워드}, 🇯🇵 일본은
{일본1위 채널}의 {일본1위 키워드} 영상이 각각 1위에 올랐는데요.
급상승 라이징 스타로는 {라이징 채널 설명}이 떠올랐습니다.
내일의 순위가 궁금하다면 저장하고 팔로우해 주세요 ✅

#유튜브순위 #유튜브이슈 #오늘의영상 #한국유튜버 #일본유튜버
#트렌드 #카드뉴스 {+ 1위 채널/키워드 해시태그 2~3개}
```

---

## 5. Canva MCP 실행 절차

1. **데이터 추출** — 1장의 규칙대로 엑셀에서 7개 영상 선정, 축약 제목·2행 설명문 작성
2. **에셋 업로드** — `upload-asset-from-url` 로 7개 썸네일 업로드
3. **디자인 생성** — `generate-design-structured` 호출
   - 포맷: Instagram Portrait Post 1080×1350, 8페이지
   - 페이지별로 카드 1~8의 배경 에셋 ID + 텍스트 블록·크기·정렬 지정
4. **후보 확인** — `get-design-candidates` → `create-design-from-candidate`
5. **수동 보정** (필요 시) — `start-editing-transaction` → `perform-editing-operations`
   - 폰트 크기 스케일 표대로 강제 지정 (자동 축소 방지)
   - 텍스트 안전영역(좌우 72px) 침범 여부 확인
6. **검수** — `get-design-thumbnail` 로 각 페이지 시안 확인 후 사용자 승인
7. **내보내기** — `export-design` (PNG, 8장) → 인스타그램 업로드용 전달 + 캡션 텍스트 함께 제공

### 품질 체크리스트

- [ ] 표지 타이틀 "오늘의 유튜브 이슈영상 순위" 96px 유지, 2행 배치
- [ ] 표지 하단 리스트에 7개 영상 제목+채널명 모두 판독 가능(32px 이상)
- [ ] 본문 제목 72px 이상, 배경과 대비비 4.5:1 이상(그라데이션으로 확보)
- [ ] 일본어 채널명 원문 표기 + 필요 시 한국어 요약 병기
- [ ] 각 카드 순위 뱃지·페이지 번호(N/8) 표기
- [ ] 썸네일 저작권: 출처(채널명) 카드 내 명시, 캡션에 해시태그 크레딧
