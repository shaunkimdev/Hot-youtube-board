# 인스타그램 카드뉴스 제작 가이드

## 기본 구조

- 카드뉴스는 `한국`, `일본`, `미국`을 각각 별도 세트로 만든다.
- 각 세트는 `표지 1장 + TOP3 영상별 3장 + 급상승 3장(있을 때만)` 구조를 따른다.
- 따라서 TOP3만 있으면 10장, 급상승까지 있으면 13장이다.

## 핵심 규칙

- 카드뉴스 3분할은 영상 자막 내용을 먼저 읽고 초반, 중반, 후반의 핵심 장면으로 나눈다.
- 각 카드의 소제목은 `초반핵심`, `중반핵심`, `후반핵심`으로 고정한다.
- 각 카드의 본문은 자막 내용을 바탕으로 한 짧은 핵심 설명 1문장으로 쓴다.
- 그 아래 보조 문장은 해당 구간의 간단 소개 1문장만 넣는다.
- `자동요약`, `미검수`, `자동 요약 미검수` 같은 문구는 카드와 캡션 어디에도 넣지 않는다.

## 이미지 규칙

- 본문 카드는 반드시 해당 구간의 실제 영상 캡처 이미지를 사용한다.
- 1장, 2장, 3장은 서로 다른 장면이어야 한다.
- 썸네일은 표지용 우선, 본문에서는 캡처 프레임이 없을 때만 예외적으로 사용한다.

## 카드 레이아웃

### 표지

- 날짜
- 국가명
- 오늘의 유튜브 이슈
- TOP3 제목 요약

### 본문 카드

- 왼쪽 상단: 국가/랭크 정보
- 배지: `1/3`, `2/3`, `3/3`
- 배지 오른쪽: 영상제목, 채널명, 조회수
- 메인 소제목: `초반핵심`, `중반핵심`, `후반핵심`
- 본문: 자막 기반 핵심 설명 1문장
- 보조 문장: 해당 구간의 간단 소개 1문장

## 정보 블록 규칙

각 영상은 카드 생성 전에 아래 정보를 먼저 정리한다.

```json
{
  "rank": 1,
  "title_full": "영상 전체 제목",
  "title_card": "카드에 노출할 짧은 제목",
  "channel": "채널명",
  "views_text": "12.3만",
  "segments": [
    {
      "label": "1/3",
      "title": "초반핵심",
      "summary": "자막을 바탕으로 뽑은 핵심 설명",
      "note": "해당 구간의 간단 소개",
      "image": "path/to/frame1.jpg"
    },
    {
      "label": "2/3",
      "title": "중반핵심",
      "summary": "자막을 바탕으로 뽑은 핵심 설명",
      "note": "해당 구간의 간단 소개",
      "image": "path/to/frame2.jpg"
    },
    {
      "label": "3/3",
      "title": "후반핵심",
      "summary": "자막을 바탕으로 뽑은 핵심 설명",
      "note": "해당 구간의 간단 소개",
      "image": "path/to/frame3.jpg"
    }
  ]
}
```

## 캡션 규칙

- 카드뉴스 캡션도 영상별 3구간 구조를 그대로 반영한다.
- 영상별로 `1/3`, `2/3`, `3/3` 요약을 순서대로 적는다.
- 카드와 동일하게 검수 전 자동요약 문구는 넣지 않는다.

## 체크리스트

- [ ] 영상 1개당 3장으로 분할됐는가
- [ ] 자막 내용을 기준으로 초반/중반/후반을 나눴는가
- [ ] 소제목이 `초반핵심`, `중반핵심`, `후반핵심`인가
- [ ] 각 장면이 실제 영상 캡처 이미지인가
- [ ] `1/3` 오른쪽에 영상제목, 채널명, 조회수가 들어갔는가
- [ ] `자동요약`, `미검수` 문구가 빠졌는가
