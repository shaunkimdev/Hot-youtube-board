# 인스타그램 카드뉴스 제작 가이드 — 오늘의 유튜브 이슈영상 순위

사용자가 유튜브 이슈분석 엑셀 파일(예: `YouTube이슈분석_개인채널_YYYY-MM-DD.xlsx`)을 지정하면,
그 파일에서 영상을 선정해 인스타그램 카드뉴스를 자동 제작하는 가이드입니다.

- **국가별로 카드뉴스를 따로 만든다.** 엑셀에는 한국·일본·미국(총 3개국) 영상이 들어있는데,
  이걸 하나의 캐러셀에 합치지 않고 **국가마다 별도의 카드뉴스(표지+TOP3)를 하나씩 제작**한다.
  즉 오늘 카드뉴스를 만들면 결과물은 3벌(한국 1벌 + 해외 2개국 각 1벌)이 된다.
  사용자가 특정 국가만 요청하면 그 국가 것만 만든다.
- **카드 문구와 캡션은 항상 한국어로 작성한다.** 원본 영상이 일본어·영어여도
  제목·소개·이유 등 모든 카드 텍스트와 인스타그램 캡션은 한국어로 번역해서 쓴다
  (채널명만 원문 표기 유지, 4장 참고). 영어/외국어 카드뉴스를 만들지 않는다.
- **제작 도구**: Canva MCP (`generate-design-structured` → `export-design`)
- **참고 디자인**: `카드뉴스_instagram 샘플/` 폴더의 tripgoing 카드뉴스 4장
  (전면 사진 배경 + 상/하단 어두운 그라데이션 + 굵은 흰색 한글 타이포)
- **레이아웃은 샘플과 동일하게** 맞춘다 — 순위 뱃지(동그라미 숫자)나 조회수·이슈점수 통계줄처럼
  샘플에 없는 요소는 카드 이미지에 넣지 않는다. 대신 샘플의 "장소명 굵은 타이틀 + 🌐 국가 태그 + 2줄 설명 +
  우상단 페이지 번호(`N/10`) + 하단 로고 워터마크" 구조를 그대로 따른다.
- **가독성 조정**: 폰트 크기는 샘플 대비 **약 20~30% 확대**를 유지한다 (아래 타이포 스케일 참조).
  즉 레이아웃 구성요소는 샘플과 동일하되, 글자 크기만 크게.
- **표지 vs 본문 카드의 배경 처리는 다르다**:
  - 표지(카드 1)는 텍스트가 많으므로 기존처럼 전체 어두운 오버레이를 깐다.
  - **본문 카드(2~8)는 썸네일이 핵심 정보이므로 사진 전체를 어둡게 가리지 않는다.**
    텍스트는 좌측 하단에 모아서 배치하고, 그 텍스트 블록 바로 뒤에만 반투명 패널을 깔아
    사진 노출 면적을 최대화한다 (아래 3장 참고).
- **문구 톤은 항상 "지금 뜨는 핫이슈 영상!" 느낌으로 쓴다.** 카드 제목·설명, 인스타 캡션
  모두 밋밋한 사실 나열이 아니라 **지금 실시간으로 화제가 되고 있다는 긴급감**이 느껴지게
  작성한다 ("지금", "실시간", "화제", "터졌다", "역대급", "난리" 같은 표현 활용). 이 시리즈의
  영상 선정 기준 자체가 **구독자대비조회수**(조회수÷구독자, 몇 배 터졌는지)이므로, 이 배수를
  카드나 캡션에 임팩트 있는 숫자로 적극 활용한다 (예: "구독자 대비 무려 32배 조회수 폭발").
  단, 표현의 온도만 높이는 것이지 **사실관계(제목·수치·내용)를 과장·왜곡하지 않는다** — 자세한
  적용 규칙은 4장 참고.

---

## 1. 입력 데이터 추출 규칙

지정된 엑셀 파일의 구조 (시트 2개):

| 시트 | 내용 | 주요 컬럼 |
|---|---|---|
| `전체` | 국가×주제별 이슈영상 | 날짜, 국가, 주제, 순위, 제목, 채널, 게시일, 길이, 조회수, 좋아요, 댓글, 이슈점수, 구독자, 구독자대비조회수, 상세요약, 도움내용, 영상링크(하이퍼링크), 분석방식 |
| `라이징스타` | 급상승 신흥 채널 영상 | 위와 동일 (이슈점수는 0일 수 있음) |

> ⚠️ **영상 선정은 이슈점수가 아니라 `구독자대비조회수`(조회수÷구독자, 배수) 기준이다.**
> `이슈점수`(조회수+가중치×좋아요·댓글 기반 옛 지표)는 참고용으로 컬럼이 남아있을 뿐,
> 정렬·선정에는 쓰지 않는다. 파이프라인 단계에서 이미 두 가지 스팸/저품질 필터를 통과한
> 영상만 순위 산정 대상이 된다: **① 조회수 10만 미만 제외**(구독자 몇 명짜리 채널이
> 우연히 몇백 회 조회되어 배율만 비정상적으로 높아지는 것을 방지), **② 좋아요 비율
> (좋아요÷조회수) 0.1% 미만 제외**(조회수만 있고 실제 참여가 없는 봇/조작 조회수 의심
> 영상 배제 — 실제 사례: 조회수 12.9만에 좋아요 8개=0.006%짜리 영상이 배율 1위로 잡혔던
> 문제를 계기로 도입). 시트에 있는 영상은 모두 이 두 기준을 이미 통과한 상태이지만, 그래도
> 100% 걸러지진 않으므로(재업로드·리뷰 영상 등은 좋아요 비율이 정상이어도 존재) 카드뉴스
> 제작 전 채널명·제목을 훑어보고 명백한 재업로드/봇성 채널이 보이면 사용자에게 알리고
> 대체 후보를 검토한다.

> ⚠️ 로컬 환경에서 openpyxl이 numpy 문제로 실패할 수 있음.
> 그 경우 xlsx를 zip으로 열어 `xl/sharedStrings.xml` + `xl/worksheets/sheetN.xml`을
> 직접 파싱하고, 영상 링크는 `xl/worksheets/_rels/sheetN.xml.rels`의 하이퍼링크에서 추출한다.

### 영상 선정 기준

1. **국가별 TOP 3**: `전체` 시트에서 `국가` 값별로 행을 나눠, 각 국가마다 **구독자대비조회수
   내림차순** 정렬 후 상위 3개를 뽑는다 (주제 구분 없이 국가 전체에서 선정). 대상 국가와 국기는:

   | 국가 | 국기 |
   |---|---|
   | 한국 | 🇰🇷 |
   | 일본 | 🇯🇵 |
   | 미국 | 🇺🇸 |

   엑셀에 없는 국가(그날 수집이 안 된 국가)는 건너뛴다. 사용자가 특정 국가만 요청하면 해당
   국가만 처리한다.
2. **⭐ 라이징스타 1개**: `라이징스타` 시트의 순위 1위 영상. 이 영상이 어느 국가 소속인지
   확인해서, **그 국가의 카드뉴스에만** 4번째 카드로 추가한다 (나머지 국가는 TOP3만으로 구성).
   사용자가 특정 국가의 라이징 영상을 원하면 그 국가의 최상위 라이징 영상으로 교체.
3. 국가마다 **별도의 카드뉴스 세트**(표지 1장 + TOP3 3장, 라이징 해당 국가는 +1장)를 만든다 —
   한국 TOP3와 일본 TOP3를 한 캐러셀에 합치던 예전 방식은 더 이상 쓰지 않는다.

### 카드 제작에 필요한 필드 (영상당)

- 제목 (원본이 외국어면 한국어로 번역 후, 길면 카드용 1줄 요약 축약본 별도 작성 — **외국어 원문을
  그대로 카드에 쓰지 않는다**)
- 채널명 (원문 표기 유지 — 일본어·영어 채널명 모두 번역하지 않고 그대로)
- 조회수 (만 단위 축약: `310.8만`)
- 구독자 (만 단위 축약, 예: `12.4만`)
- 구독자대비조회수 (배수로 표기, 예: `25.1배` — 조회수÷구독자)
- 상세요약 → 카드용 2행 설명문(38px 기준 약 40자 내외)으로 요약
- 도움내용/⏱타임라인 컬럼 → "봐야 하는 이유"(왜 볼만한지, 무엇을 얻을 수 있는지) 문장으로 정리
- 영상링크 → VIDEO_ID 추출, 원본 URL은 캡션용으로 그대로 보관
- 배경 이미지: `https://i.ytimg.com/vi/<VIDEO_ID>/maxresdefault.jpg` (404 시 `hqdefault.jpg` 폴백)

> ⚠️ **중요**: 아래 4장의 "영상별 정보 블록"이 카드뉴스 전체의 단일 소스(single source of truth)입니다.
> 카드 디자인의 제목/라벨/설명 문구와 인스타그램 캡션 문구는 반드시 이 블록에서 파생시켜 작성하고,
> 서로 내용이 어긋나지 않도록(제목 표기, 조회수 숫자, 채널명 등) 통일한다.

---

## 2. 공통 디자인 시스템

국가별로 별도 캐러셀을 만들지만, 아래 디자인 값(색상·폰트·레이아웃 규칙)은 3개국 전부 동일하게 적용한다.

| 항목 | 값 |
|---|---|
| 캔버스 | 1080 × 1350 px (인스타 4:5), **국가마다 4장 캐러셀** (표지1 + TOP3 3장; 라이징스타가 속한 국가는 5장) |
| 배경 — 표지 | 영상 썸네일 전면(full-bleed) + 상·하단 검정 그라데이션 오버레이 (위 0→40%, 아래 0→75% 불투명) |
| 배경 — 본문 카드(2~8) | 썸네일을 **어둡게 가리지 않고 선명하게 그대로 노출**. 전체 그라데이션 없음 |
| 텍스트 배경 패널 — 본문 카드(2~8) | 좌측 하단 텍스트 블록 **바로 뒤에만** 반투명 검정 패널(약 55~65% 불투명, 모서리 둥글게, 텍스트 블록 크기에 딱 맞게) |
| 헤드라인 폰트 | Pretendard Black 또는 Noto Sans KR Black, 흰색 #FFFFFF, 자간 -2% |
| 본문 폰트 | Noto Sans KR Medium, #F2F2F2 |
| 표지 글자색 | **전부 흰색 #FFFFFF로 통일** (타이틀·날짜·순위 리스트 모두 동일 색, 옐로 등 다른 색 섞지 않음) |
| 포인트 컬러 | 옐로 #FFD60A (라이징 스타 강조 전용, 그 외 카드는 흰색 위주로 샘플처럼 절제) |
| 로고 뱃지 | 좌상단 `HOT YouTube` 필박스 (검정 배경 + 흰 텍스트, 샘플의 trip going 뱃지 위치와 동일, **표지 전용**) |
| 페이지 번호 | 우상단 `N/4` (라이징 포함 국가는 `N/5`, 샘플의 `1/10` 표기 위치·스타일과 동일, 반투명 검정 필박스) |
| 하단 로고 워터마크 | 카드 중앙 하단에 작은 원형 로고 (샘플 본문 카드의 하단 중앙 로고 워터마크와 동일 위치) |
| 푸터 | 국가명 워드마크(예: `🇺🇸 USA`, 점선 없이 국기+영문 국가명 한 줄), 40px, 60% 불투명 (표지 전용) |
| 텍스트 안전영역 | 좌우 72px, 상하 96px 마진 안쪽 |

> ⚠️ 순위를 나타내는 원형 숫자 뱃지, 조회수·이슈점수 통계줄은 샘플에 없는 요소이므로 **넣지 않는다**.
> 순위 표기는 본문 카드 라벨 문구(`🇺🇸 미국 N위 · 채널명`) 하나로만 나타낸다.

> ⚠️ 본문 카드(2~8)는 사진이 더 중요하므로, 텍스트를 화면 전체에 흩뿌리지 않고
> **좌측 하단 한 곳에 제목·라벨·설명을 모아서** 배치한다. 사진을 가리는 어두운 오버레이는
> 텍스트 블록 뒤 패널 하나로 최소화하고, 그 외 영역은 썸네일이 그대로 보이게 둔다.

### 타이포 스케일 (샘플 대비 확대, 레이아웃 요소는 샘플과 동일)

| 요소 | 샘플 추정 | **적용 크기** |
|---|---|---|
| 표지 메인 타이틀 | ~72px | **108px / 행간 1.15** |
| 표지 날짜·부제 | — | **44px** |
| 표지 순위 리스트 (제목·채널명 동일 크기) | — | **40px** |
| 본문 카드 영상 제목(샘플의 "장소명") | ~56px | **72px / 행간 1.2** |
| 본문 카드 국기·순위·채널 라벨(샘플의 "🌐 국가" 태그) | ~32px | **44px** |
| 본문 카드 설명 2줄 | ~28px | **38px / 행간 1.5** |
| 페이지 번호 (`N/4` 또는 `N/5`) | ~28px | **32px** |

---

## 3. 카드별 레이아웃 (국가마다 4장, 라이징 포함 국가는 5장)

아래 템플릿을 **국가마다 독립적으로** 적용한다 (한국용 1벌, 일본용 1벌, 미국용 1벌 — 총 3벌).
서로 다른 국가의 영상을 한 캐러셀에 섞지 않는다.

### 카드 1 — 표지 (국가마다 1장)

- **배경**: 그 국가 1위 영상 썸네일 확대 + 전체 어두운 오버레이(55%)
- **상단**: 로고 뱃지 + 날짜 칩 `YYYY.MM.DD` (엑셀의 날짜 컬럼 사용, 44px, 흰색)
- **메인 타이틀** (108px, 흰색, 좌측 정렬, 샘플 표지처럼 상단 1/3 지점, 2행 배치, **한국어로 작성**):

  > **오늘의
  > 유튜브 이슈영상 순위**

  (부제로 국가명을 붙인다: 아래 날짜 줄에 `· {국기} {국가명} TOP3` 형태로 표기)

- **하단 순위 리스트** (반투명 검정 패널 위, 40px 흰색으로 통일, 각 제목은 1줄 축약 + 말줄임,
  채널명 병기, **제목은 반드시 한국어로 번역**해서 표기):

  ```
  {국기} {국가명} TOP3
  1️⃣ {1위 축약제목(한국어)} · {채널명(원문)}
  2️⃣ {2위 축약제목(한국어)} · {채널명(원문)}
  3️⃣ {3위 축약제목(한국어)} · {채널명(원문)}

  (해당 국가에 라이징스타가 있으면 한 줄 추가)
  ⭐ 라이징 | {라이징 축약제목(한국어)} · {채널명(원문)}
  ```

  > ⚠️ 날짜/부제, 순위 리스트 전부 **흰색 #FFFFFF 하나로 통일**한다. 국가와 무관하게 동일 색상 규칙.

- **푸터**: 국기+영문 국가명 워드마크(예: `🇩🇪 Germany`) + `1/4`(또는 `1/5`) 페이지 표기

### 카드 2~4 — 그 국가 1·2·3위

샘플과 달리 **사진을 가리지 않는 것을 최우선**으로 한다. 순위 뱃지·통계줄 없음:

- 배경: 해당 영상 썸네일 full-bleed, **어두운 오버레이 없이 원본 그대로 노출**
- 우상단: 페이지 번호 `N/4`(또는 `N/5`) (반투명 검정 필박스 위, 샘플의 `1/10`과 동일 스타일)
- **좌측 하단에 텍스트를 모아서 배치** (좌측 정렬, 안전영역 좌측 72px 기준), **한국어로 작성**:
  - 제목(72px Bold, 최대 2행): 축약 제목 (원본이 일본어/영어면 한국어로 번역)
  - 라벨(44px): `{국기} {국가명} N위 · {채널명}` (채널명은 원문 표기)
  - 설명(38px, 2행): `상세요약`을 한국어로 요약한 문장 — **"지금 화제인 이유"가 드러나는
    트렌디한 톤**으로 쓴다 (무미건조한 요약 대신 "지금 SNS에서 난리 난", "구독자 대비
    OO배 터진" 같은 표현 활용, 사실관계는 유지)
  - 세 요소를 서로 붙여 하나의 블록으로 묶고, 화면 전체로 흩어놓지 않는다
- 텍스트 블록 바로 뒤에만 **반투명 검정 패널**(55~65% 불투명, 모서리 둥글게, 텍스트 크기에 맞춤)을 깔아
  글자만 선명하게 보이도록 하고, 패널 밖 사진 영역은 어둡게 가리지 않는다
- 하단 중앙(텍스트 블록과 겹치지 않는 위치): 작은 원형 로고 워터마크 (샘플과 동일)

### 카드 5 — ⭐ 라이징스타 (해당 국가에만 존재)

그날의 라이징스타 영상이 이 국가 소속일 때만 추가하는 5번째 카드. 다른 국가는 카드 4장으로 끝낸다.

- 배경: 라이징 영상 썸네일, **어두운 오버레이 없이 원본 그대로 노출**
- 우상단: 페이지 번호 `5/5`
- **좌측 하단에 텍스트 클러스터**로 배치 (카드 2~4와 동일한 원칙), **한국어로 작성**:
  - 상단 태그: ⭐ + `오늘의 라이징 스타` (옐로 #FFD60A, 56px) — 라이징 카드에서만 옐로 포인트 컬러 사용
  - 제목(72px): 축약 제목 (한국어)
  - 라벨(44px): `{국기} {채널명}` (통계 없이 채널명만, 샘플 스타일)
  - 설명(38px): 상세요약 기반(한국어) + "급상승 중인 신흥 채널" 멘트 + CTA(`팔로우·저장 ✅`) —
    구독자대비조회수 배수가 있으면 "구독자 대비 OO배 터진" 식으로 수치를 살려 화제성을 강조
- 텍스트 블록 바로 뒤에만 반투명 검정 패널(55~65% 불투명)을 깔아 가독성 확보
- 하단 중앙(텍스트와 안 겹치는 위치): 작은 원형 로고 워터마크

---

## 4. 영상별 정보 블록 & 인스타그램 본문(캡션) 작성 규칙

국가별 카드뉴스를 만들기 **전에** 먼저 그 국가의 영상(3개, 라이징 포함 국가는 4개) 각각에 대해
아래 "영상별 정보 블록"을 작성한다. 이 블록이 완성되면 ① 카드 디자인의 제목/라벨/설명 문구,
② 인스타그램 캡션(본문)을 모두 이 블록 내용을 그대로 재사용해서 작성한다 — 즉 **카드뉴스 만들 때도
반드시 이 포맷을 먼저 참고**해서 카드별 문구를 뽑아낸다 (같은 영상인데 카드와 캡션의 제목·조회수·
채널명 표기가 달라지지 않게 함).

### 4-1. 영상별 정보 블록 포맷 (국가별로 3~4개 영상 각각 작성)

깔끔·간단·명료하게, 아래 7개 항목만 **전부 한국어로** 담는다 (원본이 외국어여도 번역):

```
{국기} [{국가명} N위 또는 ⭐ 라이징] {영상 제목(한국어)}
📺 채널: {채널명(원문 표기)}
👀 조회수: {N}만
🚀 구독자대비조회수: {구독자대비조회수}배 (구독자 {구독자}명, 값 없으면 이 줄 생략)
📝 소개: {상세요약을 1~2문장으로 압축, 한국어}
✅ 봐야 하는 이유: {도움내용/⏱타임라인 컬럼 기반, 왜 볼만한지·무엇을 얻는지, 한국어}
🔗 링크: {영상링크 원본 URL}
```

작성 규칙:
- **제목**: 원본이 외국어면 먼저 한국어로 번역하고, 그래도 길면(20자 이상) 핵심만 남기고 축약
  (의미 왜곡 금지). **외국어 원문을 카드나 캡션에 그대로 쓰지 않는다.**
- **구독자대비조회수**: 엑셀의 `구독자`·`구독자대비조회수` 컬럼 값을 그대로 사용 (구독자
  비공개거나 값이 없으면 이 줄 자체를 생략, 억지로 계산하지 않는다)
- **소개**: 상세요약 컬럼에서 사실 관계만 뽑아 1~2문장으로 쓰되, **"지금 뜨는 핫이슈" 톤**
  (긴급감·화제성)을 살린다 — 위 개요의 톤앤매너 규칙 참고. 허위·과장 없이 표현의 온도만 높인다
- **봐야 하는 이유**: 도움내용/⏱타임라인 컬럼의 핵심 포인트를 "~해서 도움이 됩니다" 식 1문장으로 정리.
  클릭베이트·과장 경고(⚠️)가 있으면 "다만 ~한 점은 감안하고 볼 것" 식으로 짧게 반영
- **링크**: 반드시 영상링크 컬럼의 실제 URL 사용 (하이퍼링크 파싱 필요, 1장 참고)
- 국가 하나당 3개(라이징 포함 국가는 4개) 영상을 동일한 포맷으로 작성, 순서는 1→3위 → (라이징)

### 4-2. 인스타그램 게시물 본문(캡션) 템플릿 (국가마다 별도 캡션 1개)

인트로 후크 + 4-1에서 작성한 그 국가의 정보 블록(3~4개)을 순서대로 나열 + 해시태그로 구성한다.
**전부 한국어로 작성** (해외 국가 영상이라도 캡션은 한국어).

```
지금 {국가명}에서 가장 화제인 유튜브 영상 TOP3, 놓치면 후회합니다 🔥
{국기} {국가명}은 {1위 채널}의 {1위 키워드} 영상이 지금 실시간으로 터지며 1위에 올랐는데요.
(해당 국가에 라이징이 있으면) 특히 급상승 라이징 스타 {라이징 채널}은 구독자 대비 조회수가
폭발적으로 터진 화제의 신흥 채널입니다.
내일은 또 어떤 영상이 터질지, 지금 저장하고 팔로우해서 가장 먼저 확인하세요 ✅

—

{국기} {국가명} 1위
{영상 제목(한국어)}
📺 채널: {채널명}
👀 조회수: {N}만
🚀 구독자대비조회수: {구독자대비조회수}배 (값 없으면 생략)
📝 소개: {소개 문장 — "지금 뜨는 핫이슈" 톤}
✅ 봐야 하는 이유: {이유 문장}
🔗 링크: {URL}

(… {국가명} 2·3위, 있으면 ⭐ 라이징 순서로 동일 블록 반복 …)

—

#유튜브순위 #유튜브이슈 #오늘의영상 #{국가명}유튜버
#트렌드 #카드뉴스 {+ 1위 채널/키워드 해시태그 2~3개}
```

> 캡션은 그 국가의 카드뉴스 PNG와 함께 항상 세트로 제공한다 (카드 이미지만 던지고 끝내지 않음).
> 국가 3벌을 모두 만들면 캡션도 국가마다 별도 파일로 3개가 나온다.

### 4-3. 캡션 저장 방식

- 완성된 캡션(4-2 템플릿)은 **채팅에 텍스트로만 출력하지 말고, `.docx` 워드 파일로 저장**한다.
- 폴더 구조: `카드뉴스_YYYY-MM-DD/{국가명}/` — **국가마다 하위 폴더를 따로 만든다**.
  예: `카드뉴스_2026-07-06/한국/`, `카드뉴스_2026-07-06/미국/` 등 3개 하위 폴더.
- 각 국가 폴더 안에 그 국가의 카드 PNG(표지+TOP3, 라이징 있으면 +1장)와 캡션 파일을 나란히 둔다.
- 파일명: `카드뉴스_YYYY-MM-DD/{국가명}/캡션_{국가명}.docx`
- python-docx(또는 동등 라이브러리)로 생성. 로컬에 없으면 설치 후 생성한다.
- 워드 파일 안에는 4-2 템플릿 내용(인트로 후크 + 그 국가 정보 블록 + 해시태그)을 그대로 담는다.
  줄바꿈·이모지는 원문 그대로 유지.

#### ⚠️ 한글·이모지 폰트 깨짐 방지 (필수)

**원인**: python-docx의 `Document()` 기본 템플릿은 런(run) 단위에 폰트를 전혀 지정하지 않는다.
전부 `Normal` 스타일 → 테마 폰트(`docDefaults`)에만 의존하는데, 그 `docDefaults`의
`w:lang`이 `eastAsia="en-US"`로 박혀 있고(한국어 아님), 이모지(📺👀📝✅🔗⭐🇰🇷🇯🇵 등)는
테마의 동아시아/라틴 폰트 매핑표에 아예 글꼴이 없다. 그 결과 워드/뷰어에 따라 한글이
엉뚱한 폰트로 렌더링되거나 이모지가 네모(tofu)로 깨져 보인다.
(실제로 2026-07-05 캡션 파일에서 이 문제가 발생해 확인함 — 모든 run에 `rPr`/`rFonts`가
전혀 없이 저장되어 있었다.)

**수정 방침**: 문서 생성 시 아래처럼 **모든 run에 한글 폰트를 명시적으로 박아 넣는다**
(테마 상속에 의존하지 않음):

```python
from docx import Document
from docx.shared import Pt
from docx.oxml.ns import qn

FONT = "맑은 고딕"  # Malgun Gothic — Windows 기본 탑재, 한글 완비

def set_korean_font(run, name=FONT):
    run.font.name = name  # w:ascii / w:hAnsi
    rPr = run._element.get_or_add_rPr()
    rFonts = rPr.find(qn('w:rFonts')) or rPr.makeelement(qn('w:rFonts'), {})
    if rFonts not in rPr:
        rPr.insert(0, rFonts)
    rFonts.set(qn('w:eastAsia'), name)   # ← 이 줄이 핵심 (동아시아 폰트 명시)
    rFonts.set(qn('w:cs'), name)
    lang = rPr.find(qn('w:lang')) or rPr.makeelement(qn('w:lang'), {})
    if lang not in rPr:
        rPr.append(lang)
    lang.set(qn('w:eastAsia'), 'ko-KR')  # ← en-US 오류 수정

# Normal 스타일에도 동일하게 적용 (문서 기본값 자체를 고침)
style = doc.styles['Normal']
style.font.name = FONT
# ... 위와 동일한 rFonts/eastAsia/lang 처리를 style.element에도 적용

# 문단 추가 시 항상 run에 적용
para = doc.add_paragraph()
run = para.add_run(text)
set_korean_font(run)
```

- 새 파일을 만들 때마다 이 헬퍼로 **모든 run**에 폰트를 지정한다 (Normal 스타일만 건드리고
  run에 안 넣으면 일부 뷰어에서 다시 깨질 수 있음).
- 저장 시도 시 `PermissionError`/`Device or resource busy`가 나면 **해당 .docx가 다른
  프로그램(Word 등)에서 열려 있다는 뜻**이므로, 사용자에게 파일을 닫아달라고 요청한 뒤
  다시 저장한다. 스크래치패드에 임시 저장 후 닫힘을 확인하고 옮기는 것도 방법.

---

## 5. Canva MCP 실행 절차

이 절차를 **국가마다 반복**한다 (한국 → 일본 → 미국, 또는 사용자가 지정한 국가만).

1. **데이터 추출 & 정보 블록 작성** — 1장의 규칙대로 엑셀에서 그 국가의 3개(라이징 포함 국가는
   4개) 영상 선정 후, **4장의 "영상별 정보 블록" 포맷으로(한국어로) 정리를 먼저 완료**한다.
   이후 카드 문구(축약 제목·2행 설명문)와 캡션은 이 블록에서 그대로 가져다 쓴다.
2. **에셋 업로드** — `upload-asset-from-url` 로 그 국가의 썸네일 3~4개 업로드
3. **디자인 생성** — `generate-design-structured` 호출
   - 포맷: Instagram Portrait Post 1080×1350, 4페이지(라이징 포함 국가는 5페이지)
   - 페이지별로 배경 에셋 ID + 텍스트 블록·크기·정렬 지정
4. **후보 확인** — `get-design-candidates` → `create-design-from-candidate`
5. **수동 보정** (필요 시) — `start-editing-transaction` → `perform-editing-operations`
   - 폰트 크기 스케일 표대로 강제 지정 (자동 축소 방지)
   - 텍스트 안전영역(좌우 72px) 침범 여부 확인
6. **검수** — `get-design-thumbnail` 로 각 페이지 시안 확인 후 사용자 승인
7. **내보내기** — `export-design` (PNG) → `카드뉴스_YYYY-MM-DD/{국가명}/` 폴더에 저장
8. **캡션 파일 생성** — 4-3 규칙대로 캡션을 `.docx`로 만들어 같은 `{국가명}/` 폴더에 저장
   (캡션을 채팅 텍스트로만 주고 끝내지 않는다)
9. 다음 국가로 넘어가 1~8 반복. 모든 국가가 끝나면 사용자에게 3벌(또는 지정한 국가 수) 전체 요약 보고

### 품질 체크리스트

- [ ] 요청받은 국가 전부(또는 지정된 국가) 각각 별도 카드뉴스로 완성됨 — 서로 다른 국가를
      한 캐러셀에 섞지 않았는지 확인
- [ ] 모든 카드 텍스트·캡션이 **한국어**로 작성됨 (원본이 일본어/영어여도 번역,
      채널명만 원문 표기)
- [ ] 표지 타이틀 "오늘의 유튜브 이슈영상 순위" 108px 유지, 2행 배치
- [ ] 표지 글자(타이틀·날짜·순위 리스트)가 전부 흰색으로 통일되어 있음 (색 혼용 없음)
- [ ] 표지 하단 리스트에 그 국가 영상 제목+채널명 모두 판독 가능(40px)
- [ ] 본문 카드 사진에 전체 어두운 오버레이가 없고 원본 그대로 선명하게 보임
- [ ] 본문 카드 텍스트가 좌측 하단 한 곳에 모여 있고, 사진을 가리는 면적이 최소화됨
- [ ] 텍스트 블록 바로 뒤에만 반투명 패널이 있어 글자 가독성 확보 (배경 전체가 아님)
- [ ] 본문 제목 72px 이상, 텍스트 뒤 반투명 패널로 대비비 4.5:1 이상 확보
- [ ] 외국어 채널명 원문 표기 + 필요 시 한국어 설명 병기
- [ ] 순위 뱃지·통계줄 없이 샘플과 동일한 레이아웃(제목+국기 라벨+2줄 설명+우상단 페이지 번호+하단 워터마크)
- [ ] 썸네일 저작권: 출처(채널명) 카드 내 명시, 캡션에 해시태그 크레딧
- [ ] 국가별 정보 블록(제목/채널/조회수/구독자대비조회수/소개/봐야 하는 이유/링크) 작성 완료
- [ ] 카드 문구와 캡션 정보 블록의 제목·조회수·채널명 표기가 서로 일치
- [ ] 카드 설명·캡션 후킹 문구가 "지금 뜨는 핫이슈" 톤(긴급감·화제성)으로 쓰였고, 값이 있으면
      구독자대비조회수 배수를 임팩트 있게 활용함 (과장·허위 없이 사실관계는 정확)
- [ ] 캡션이 `카드뉴스_YYYY-MM-DD/{국가명}/캡션_{국가명}.docx`로 저장됨 (그 국가 이미지와 같은 폴더)
- [ ] 캡션 .docx의 모든 run에 한글 폰트(예: 맑은 고딕)와 `eastAsia`/`lang` 속성이 명시적으로
      설정됨 (4-3 "한글·이모지 폰트 깨짐 방지" 코드 사용, 테마 상속에 의존하지 않음)
